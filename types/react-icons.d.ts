declare module "react-icons/hi" {
  import { ComponentType, SVGAttributes } from "react";
  export const HiMenu: ComponentType<SVGAttributes<SVGElement>>;
  // 다른 HI 아이콘들도 필요하다면 추가
}

declare module "react-icons/fc" {
  import { ComponentType, SVGAttributes } from "react";
  export const FcGoogle: ComponentType<SVGAttributes<SVGElement>>;
  // 다른 FC 아이콘들도 필요하다면 추가
}
